[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "video-duperz"
version = "0.1.0"
description = "Windows-first PySide app for perceptual duplicate video detection."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "MT0z", email = "mt0z1@pir0z.yxz" }]
requires-python = ">=3.13"
keywords = []
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "numpy>=2.4.2",
  "opencv-python>=4.13.0.92",
  "PySide6>=6.10.2",
]

[project.urls]
Homepage = "https://github.com/Threepwood-7/video-duperz"
Repository = "https://github.com/Threepwood-7/video-duperz"
Issues = "https://github.com/Threepwood-7/video-duperz/issues"

[tool.hatch.env]
installer = "uv"

[tool.hatch.build.targets.wheel]
packages = ["src/video_duperz"]

[tool.hatch.envs.default]
dependencies = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "pytest-qt>=4.4",
  "PySide6>=6.10.2",
  "qbittorrent-api>=2025.11.1",
]

[tool.hatch.envs.default.scripts]
test = "pytest {args:tests}"
test-cov = "pytest --cov=src/video_duperz --cov-report=term-missing --cov-report=xml {args:tests}"

[tool.hatch.envs.lint]
dependencies = [
  "ruff>=0.6",
  "basedpyright>=1.18",
  "deptry>=0.24",
  "vulture>=2.15",
]

[tool.hatch.envs.lint.scripts]
check = "ruff check ."
fmt = "ruff format --check ."
fix = ["ruff check --fix .", "ruff format ."]
types = "basedpyright src/video_duperz"
deps = "deptry ."
deadcode = "vulture src --min-confidence 80"
complexity = "ruff check src --select C901 --output-format concise"
policy = "python scripts/policy/check_standard.py"
all = [
  "ruff check --fix .",
  "ruff format .",
  "basedpyright src/video_duperz",
  "python scripts/policy/check_standard.py",
]

[tool.hatch.envs.test]
dependencies = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "pytest-qt>=4.4",
  "PySide6>=6.10.2",
  "qbittorrent-api>=2025.11.1",
]

[[tool.hatch.envs.test.matrix]]
python = ["3.13"]

[tool.hatch.envs.test.scripts]
run = "pytest {args:tests}"

[tool.ruff]
target-version = "py313"
line-length = 88
src = ["src"]

[tool.ruff.lint]
select = [
  "E",
  "W",
  "F",
  "I",
  "N",
  "B",
  "C4",
  "UP",
  "SIM",
  "TCH",
  "RUF",
]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*" = ["S101", "S105", "S106"]

[tool.ruff.lint.isort]
known-first-party = ["video_duperz"]

[tool.ruff.lint.pep8-naming]
ignore-names = [
  "activateWindow",
  "closeEvent",
  "eventFilter",
  "filterAcceptsRow",
  "headerData",
  "highlightBlock",
  "isActive",
  "isRunning",
  "keyPressEvent",
  "lessThan",
  "mouseReleaseEvent",
  "paintEvent",
  "requestInterruption",
  "setApplicationDisplayName",
  "setApplicationName",
  "setOrganizationName",
  "showEvent",
  "showNormal",
  "windowState",
]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
docstring-code-format = true

[tool.deptry]
known_first_party = ["video_duperz"]
package_module_name_map = { "PySide6" = "PySide6", "qbittorrent-api" = "qbittorrentapi", "opencv-python" = "cv2" }

[tool.basedpyright]
pythonVersion = "3.13"
pythonPlatform = "All"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"
include = ["src"]
exclude = ["tests", "**/__pycache__"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-ra", "--strict-markers", "--import-mode=importlib"]
xfail_strict = true

[tool.coverage.run]
source = ["src/video_duperz"]
branch = true
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = false
